=== ✅ תיקנתי את שגיאת ההזחה! ===

## 🔧 הבעיה:

בקובץ `overlay_renderer.py` שורות 106-109 היו עם הזחה כפולה (16 רווחים במקום 8).

```python
# ❌ לפני (הזחה כפולה):
                radius_x = max(int(w * 0.9), 40)
                radius_y = max(int(w * 0.4), 20)

# ✅ אחרי (הזחה נכונה):
        radius_x = max(int(w * 0.9), 40)
        radius_y = max(int(w * 0.4), 20)
```

---

## ✅ תוקן!

הקובץ עכשיו עובד ללא שגיאות.

---

## 🚀 נסה עכשיו:

### פשוט לחץ כפול: `run.bat`

---

## 🎯 התוכנה צריכה להיפתח עכשיו!

אם היא נפתחת:
1. ➕ Add Videos
2. בחר סרטון
3. לחץ על הסרטון ברשימה
4. ➕ Add Marker
5. צייר bbox
6. בחר: ⭕ 3D Floor Hoop
7. Confirm

---

## 📸 בדוק את העיגול:

האם הוא נראה כמו בתמונה שלך?
- עיגול גדול
- שחקנית במרכז
- רגליים במרכז העיגול

---

**נסה עכשיו! התוכנה צריכה לעבוד! 🎯**


